# backend service base url (unused at runtime: the API base url is read from public/settings.json;
# kept because the template's vite proxy plumbing reads it)
VITE_SERVICE_BASE_URL=http://localhost:13223

# other backend service base url
VITE_OTHER_SERVICE_BASE_URL=`{}`
