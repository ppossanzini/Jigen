import type { Metadata } from "next";
import { CodeBlock, DetailShell } from "../_components/DetailShell";
export const metadata: Metadata = { title: "Benchmarks — Jigen DB", description: "Jigen benchmark methodology, latency, throughput and architectural context." };
const command = `cd tests/ComparativeBenchmarks
dotnet run -c Release -- generate

# ingest → flush → pre-index → build → post-index
dotnet run -c Release -- macro \
  jigendb-lazy-w4,qdrant,milvus,pgvector \
  random-10k`;
export default function BenchmarksPage(){return <DetailShell eyebrow="PERFORMANCE, WITH CONTEXT" index="05 / 05" title="Measure the architecture, not only the algorithm." intro="Jigen's comparative benchmark deliberately contrasts an embedded library with containerized services. The numbers are useful when the system boundary behind them remains visible.">
  <section className="detailSection wrap"><div className="metricStrip"><article><small>SEARCH P50</small><strong>0.19<span> ms</span></strong><p>Jigen lazy → HNSW</p></article><article><small>INGEST</small><strong>95,168<span> vec/s</span></strong><p>10k × 128 dimensions</p></article><article><small>RECALL@10</small><strong>1.000</strong><p>Post-index dataset result</p></article><article><small>INDEX BUILD</small><strong>0.03<span> s</span></strong><p>Lazy reconciliation</p></article></div><p className="contextNote">These are indicative results from one 22-core laptop with NVMe storage on Linux. Re-run the suite on your hardware and data before making a production decision.</p></section>
  <section className="detailBand"><div className="wrap"><div className="detailHeading"><span>01</span><div><h2>Same lifecycle, visible boundaries</h2><p>Every backend passes through the same six stages. External systems still pay serialization, protocol and container overhead; Jigen embedded does not.</p></div></div><div className="benchmarkFlow">{['INGEST','FLUSH','PRE-INDEX','BUILD INDEX','500 QUERIES','DELETE + STATS'].map((item,i)=><article key={item}><b>0{i+1}</b><span>{item}</span></article>)}</div></div></section>
  <section className="detailSection wrap"><div className="detailHeading"><span>02</span><div><h2>Latency after index construction</h2><p>P50 search latency on 10k random unit vectors, 128 dimensions. Lower is better.</p></div></div><div className="largeChart">{[['JIGEN',0.19,3],['QDRANT',1.18,16],['MILVUS',5.99,79],['PGVECTOR',7.60,100]].map(([name,value,width])=><div key={String(name)} className={name==='JIGEN'?'hot':''}><label>{name}</label><i><span style={{width:`${width}%`}} /></i><strong>{value} ms</strong></div>)}</div><div className="benchmarkCaveat"><b>Why the gap?</b><p>Two effects compound: Jigen defers graph construction during ingest, and embedded queries are direct calls to memory-mapped data. The comparison is architecture plus implementation—not a claim that every deployment should be embedded.</p></div></section>
  <section className="detailBand"><div className="wrap splitDetail"><div><div className="eyebrow"><span /> REPRODUCE IT</div><h2>Run the same lifecycle yourself.</h2><p>The comparative suite includes adapters for Jigen, Qdrant, Milvus and pgvector, dataset generation and separate macro and micro modes.</p><a className="button inverse" href="https://github.com/ppossanzini/Jigen/blob/main/docs/benchmarks.md">Read full methodology ↗</a></div><CodeBlock label="TERMINAL">{command}</CodeBlock></div></section>
</DetailShell>}
